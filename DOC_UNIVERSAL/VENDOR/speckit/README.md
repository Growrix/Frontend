# SpecKit Vendor Slot

Place a fresh, unmodified SpecKit copy here when needed.

## Rule

- Keep vendor material separate from `CORE/` and `TEMPLATES/`.
- Do not treat vendor files as source of truth.
- Reuse ideas intentionally; do not inherit workflow blindly.